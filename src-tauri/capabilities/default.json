{
  "identifier": "default",
  "description": "Default capabilities for the main window",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "dialog:allow-open"
  ]
}
