{
  "name": "llm-brain",
  "version": "0.3.0",
  "private": true,
  "description": "有向记忆图 + 多角色 LLM 类脑智能体系统",
  "scripts": {
    "dev:frontend": "cd frontend && bun run dev",
    "dev:backend": "cd backend && bun run dev",
    "dev": "concurrently \"bun run dev:backend\" \"bun run dev:frontend\"",
    "build:frontend": "cd frontend && bun run build",
    "build:backend": "cd backend && bun run build",
    "build:backend:sidecar": "bun run scripts/build-backend-sidecar.mjs",
    "build": "bun run build:backend && bun run build:frontend",
    "tauri": "tauri",
    "tauri:dev": "bun run build:backend && bun run build:backend:sidecar && tauri dev",
    "tauri:build": "bun run build:backend && bun run build:backend:sidecar && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/cli": "^2.10.1",
    "concurrently": "^9.1.2"
  }
}
